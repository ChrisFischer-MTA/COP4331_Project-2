import React from 'react';
import PageTitle from '../PageTitle';
import admin from './admin';

const AdPage = () => {
    return (
        <div>
            <admin/>
        </div>
    );
};

export default AdPage;